# 🌀 Helix Collective v14.5 - Railway Configuration (FIXED)
# This file configures how Railway builds and deploys the application

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# DO NOT specify startCommand - let Dockerfile CMD handle it!
# Railway will use: CMD ["python", "backend/main.py"] from Dockerfile
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Environment variables for Railway deployment
[[deploy.environmentVariables]]
name = "PYTHONUNBUFFERED"
value = "1"
