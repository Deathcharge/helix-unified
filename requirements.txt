# Helix Collective v17.0 - Consolidated Python Dependencies
# Merged from Manus 5 and Manus 6 updates
# Python 3.13 Compatible

# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.30.5
pydantic==2.10.3  # Python 3.13 compatible + security fixes
python-dotenv==1.0.1
pyyaml==6.0.1
toml==0.10.2

# Security & Authentication
passlib[bcrypt]==1.7.4  # Password hashing with bcrypt
pyjwt==2.8.0  # JWT token handling
slowapi==0.1.9  # Rate limiting for FastAPI

# API Clients & HTTP
aiohttp==3.12.14
httpx==0.28.0
requests==2.32.4
openai
anthropic>=0.39.0  # Claude API for consciousness processing

# Discord Bot
discord.py>=2.3.2  # Discord bot for consciousness orchestration

# Data Processing & Science
numpy==1.26.4
pandas==2.2.3
scipy==1.13.0
matplotlib==3.8.3
seaborn==0.13.1
plotly==5.18.0
numba==0.58.1
scikit-learn>=1.3.0  # Required by Grok Agent for ML analytics
cmdstanpy==1.2.2  # Required by Prophet for time series forecasting
prophet  # Time series forecasting for Grok Agent trend analysis

# Streamlit & Dashboard
streamlit==1.40.0
streamlit-option-menu==0.3.12

# Integrations
notion-client==2.5.0
mega.py
pycryptodome==3.20.0

# Real-time & WebSockets
websockets==13.0  # For WebSocket connections in Live Stream and Discovery Protocol
sse-starlette==2.2.1  # Server-Sent Events for real-time consciousness streaming

# Media & Audio
Pillow==10.4.0  # Updated for Python 3.13 support + security fixes
librosa==0.10.0
soundfile==0.12.1
pydub==0.25.1  # Audio processing (requires ffmpeg system package)

# Logging
python-json-logger==2.0.7
loguru==0.7.2  # Advanced logging for Zapier integration

# Cloud Storage (Optional - will gracefully degrade if not installed)
boto3>=1.28.0  # Backblaze B2 and AWS S3 storage
# webdav3>=3.14.0  # Nextcloud WebDAV storage (not available for Python 3.11+)

# AI Music Generation (Optional - requires HUGE downloads ~2GB, only for music features)
# torch>=2.0.0  # PyTorch for MusicGen model
# transformers>=4.30.0  # Hugging Face Transformers for MusicGen
# scipy>=1.10.0  # Required by MusicGen audio processing

# Database
sqlalchemy==2.0.25  # Database ORM for user management

# Optional: Commented out dependencies
# opencv-python==4.9.0.80
# sqlalchemy==2.0.25

# ============================================================================
# TESTING & QUALITY ASSURANCE (Dec 15 Launch)
# ============================================================================

# Testing Framework
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-json-report>=1.5.0
pytest-timeout>=2.2.0

# Mocking & Fixtures
responses>=0.24.1
faker>=20.1.0

# Security Testing
cryptography>=41.0.7
pyjwt>=2.8.1
bcrypt>=4.1.1

# Authentication
python-jose>=3.3.0
passlib>=1.7.4

# API Integrations for Testing
stripe>=7.8.0
sendgrid>=6.10.0

# Database Testing
sqlalchemy>=2.0.23
alembic>=1.13.1
psycopg2-binary>=2.9.9

# Code Quality
coverage>=7.3.2
pylint>=3.0.3
black>=23.12.1
isort>=5.13.2

# Production Server
gunicorn>=21.2.0
